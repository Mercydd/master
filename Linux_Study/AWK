1)awk '{print $1, $3}' file.txt   --- Вывод 1 и 3 столбца в файле

2)awk -F ',' '{print $1, $2}' file.csv  --- Вывод 1 и 3 столбца игнорирую знак ","

3)awk '$3 > 50' file.txt  --- Вывод содержимого в стобце 3 что выше 50

4)awk '{print NR, $0}' file.txt   ---  Выводит количество всех строк и паралельно весь файл (команда была поданна через запятую)
  NR - Встроенное значение, которое показывает количество строк
  
5)awk '{sum += $3} END {print "Total:", sum}' file.txt --- Первое добавляет переменную sum, которая является 3 столбцем, но добавляет сама себя, END - окончание первой переменной, 2 функция вывод теста и позже передача переменной sum

6)awk '/Alice/' file.txt --- Выводит линию в которой есть текст "Alice"

			HARD LEVEL
 awk '{sum += $3; count += 1} END {if (count > 0) print "Average:", sum / count}' file.txt --- 
1)sum это 3 столбец который сам себя плюсует
2) count += 1 количество строк (+=1 потому что начинается с 0) 

 awk 'NR == 1 || $3 > max {max = $3; maxLine = $0} NR == 1 || $3 < min {min = $3; minLine = $0} END {print "Max:", maxLine; print "Min:", minLine}' file.txt

1)

